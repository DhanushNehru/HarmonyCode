/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Note: swcMinify is deprecated and enabled by default in Next.js 13+
  // Removed to fix the invalid config warning
}

module.exports = nextConfig
